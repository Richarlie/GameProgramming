# 数据的输入
### 1.getline函数(string)
`getline`是C++中用于从输入流读取一行文本的常用函数。它有两种主要形式：
```C++
std::string line;
std::getline(std::cin, line);  // 从标准输入读取一行到字符串line 

<!--stackedit_data:
eyJoaXN0b3J5IjpbMTI4MzI4MzIxNl19
-->