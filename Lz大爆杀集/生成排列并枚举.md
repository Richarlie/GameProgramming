# 生成排列并枚举
1.**枚举全部排列**
```C++
#include<cstdio>
#include<algorithm>
using namespace std;
int main(){
    int n, P[100];
    scanf("%d",&n);
    for(int i=0; i<n; i++) scanf("%d",P+i);
    sort(P,P+n);
    do{
        for(int i=0; i<n; i++) printf("%d ",P[i]);
        printf("\n");
    }while(next_permutation(P, P+n));
    return 0;
}
```
2.**用递归方法**
其思想就是：先输出所有以1为开头的排列（递归调用），然后输出以2为开头的排列（递归调用）······以此类推到以n为开头的排列
```C++

```


<!--stackedit_data:
eyJoaXN0b3J5IjpbLTE2MTYzNTkyNjEsLTU4MjcyMjcwN119
-->