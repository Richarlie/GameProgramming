# 二分插入函数&二分查找
- 无STL
```C++
#include <vector>

template<typename T>
void binary_insert_manual(std::vector<T>& vec, const T& item) {
    if (vec.empty()) {
        vec.push_back(item);
        return;
    }
    
    size_t left = 0;
    size_t right = vec.size();
    
    // 二分查找确定插入位置
    while (left < right) {
        size_t mid = left + (right - left) / 2;
        if (vec[mid] < item) {
            left = mid + 1;
        } else {
            right = mid;
        }
    }
    
    // 在找到的位置插入元素
    vec.insert(vec.begin() + left, item);
}
```
- 有STL
``` C++
#include <vector>
#include <algorithm> // 用于std::lower_bound

// 使用二分查找确定插入位置并插入元素
template<typename T>
void binary_insert(std::vector<T>& vec, const T& item) {
    // 使用lower_bound找到第一个不小于item的位置
    auto it = std::lower_bound(vec.begin(), vec.end(), item);
    // 在该位置插入元素
    vec.insert(it, item);
}
```


<!--stackedit_data:
eyJoaXN0b3J5IjpbODA2ODMyMzczLDE1MDkzMTc3NjIsLTE3Mz
k0OTQwMF19
-->