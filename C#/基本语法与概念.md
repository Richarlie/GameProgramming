# C#基础语法与概念
1. 读取与输出
``` C#
static void Main(string[]args){//"static"代表静态
string str;
str=Console.ReadLine();
Console.OutLine(str);//输入和输出一行数据
}
```
2. 字符串
```  C#
string str="Hello World!";

int strLength=str.Length; //取字符串长度

int strIndex=str.IndexOf("llo");
 //strIndex=2 查找字符串中的第一个匹配项的从零开始的索引

string str2=str.Replace("Hello","你好");
//str2="你好,World!";

string str3=str.Substring(6,2);
//取下标为6开始的长度为2的子字符串

int num-1=114514;
string str-1=num.ToString();
//数字对应转化成字符串
string str-2="5";
int num-2=int.Parse(str-2);
long num-3=long.Parse(str-2);
//字符串对应转化成数字

int result=3;
string inputStr=“val”;
string str1=inputStr + "的计算结果为:" + result;
stirng str2=$"{inputStr}的计算结果{result}"
//像是代入填空
//上下两种的输出结果是一样的。
//字符串格式化 $"info={a}"
```

3. 关于输入与输出
``` C#
Console.WriteLine("Hello  World!"); //输出并换行
Console.Write("Hello  World!"); //输出但不换行
Console.ReadLine(); //检测输入的字符串
Console.ReadKey(); //检测键盘输入
```
<!--stackedit_data:
eyJoaXN0b3J5IjpbLTIwNTg1NTE2MiwtNDU1NTczNjUyLC01OD
UyNTY1ODksMTY0MDY2NTYzNiwtMTgwNzg2MzIzMiwtNTg0MDY0
MDY3XX0=
-->